# Database
POSTGRES_DB=brandforge
POSTGRES_USER=brandforge
POSTGRES_PASSWORD=change_this_strong_password

# App
DATABASE_URL=postgresql://brandforge:change_this_strong_password@db:5432/brandforge
GEMINI_API_KEY=your-gemini-api-key
PORT=3000
NODE_ENV=production

# Storage
STORAGE_PROVIDER=local
STORAGE_LOCAL_PATH=/data

# Auth
AUTH_MODE=none

# Jobs
JOB_CONCURRENCY=3

# Frontend (set during UI build)
VITE_API_URL=/api
